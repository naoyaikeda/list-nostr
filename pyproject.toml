[project]
name = "list-nostr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pynostr>=0.7.0",
    "python-dotenv>=1.2.1",
    "pyyaml>=6.0.3",
    "wheel>=0.46.3",
]
